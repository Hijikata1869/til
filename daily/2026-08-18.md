User.whereで取得したものはRelation、User.findで取得したものはインスタンス。だから前者ではUserのインスタンスメソッドが使えない。
今回、whereで取得した変数に.reading_notesや.tasksを実行してもそんなメソッドは無いというエラーが出た。なぜならRelationに対してメソッドを読んでしまっていたから。これらはUserのインスタンスメソッドなので、Userクラスのインスタンスに対してでないと呼び出せない。
